---
phase: 2
plan: "02-03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - .zshrc
  - .planning/phases/02-architecture-restructuring/02-DISCUSSION-LOG.md
autonomous: true
requirements:
  - FIX-08
  - FIX-09
  - ARCH-06
  - ARCH-07
---

<objective>
Fix Zap plugin order (**fast-syntax-highlighting** last), add **Docker completion fpath** before `compinit`, move **Starship** init to satisfy ARCH-06 (last tool `eval`), and source **`~/.zshrc.local`** when present for machine-specific overrides.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T1 | Reordering plugins breaks completion widgets | medium | After edits, `zsh -i -c 'zle -l | head -5'` smoke or document in log if empty in batch |
| T2 | Docker fpath points at missing directory | low | Guard `[[ -d ... ]]` before `fpath+=` |
| T3 | `.zshrc.local` executes untrusted code | medium | Document user responsibility; file is opt-in by user only |
**Block on:** high.
</threat_model>

<must_haves>
- In `.zshrc`, the line loading **fast-syntax-highlighting** appears **after** all other `plug` lines for Zap plugins
- Before `compinit`, a guarded `fpath+=` for Homebrew Docker completions exists **or** discussion log states Docker completion path absent on this machine
- `eval "$(starship init zsh)"` runs **after** `fzf`, `zoxide`, and cargo/bun sources (i.e., last among interactive tool inits in the tools section) — adjust if ARCH-06 conflicts with Phase 3 vi-order; document deviation
- `[[ -f "$HOME/.zshrc.local" ]] && source "$HOME/.zshrc.local"` appears at end of `.zshrc` (after PATH dedupe tail **or** immediately before dedupe — pick one and document; prefer **after** dedupe so local can override PATH)
</must_haves>

<tasks>

<task id="1" type="execute">
<read_first>
- `~/dotfiles/.zshrc` (Zap plug lines ~132–140)
- `~/dotfiles/.planning/REQUIREMENTS.md` (FIX-08, FIX-09)
</read_first>
<action>
Reorder **`plug`** declarations so **`zdharma-continuum/fast-syntax-highlighting`** is the **last** `plug` call. Do not change plugin semantics beyond order.
</action>
<acceptance_criteria>
- Last `plug "` line in `.zshrc` contains `fast-syntax-highlighting`
</acceptance_criteria>
</task>

<task id="2" type="execute">
<read_first>
- `~/dotfiles/.zshrc` (region immediately before `autoload -Uz compinit`)
</read_first>
<action>
Before `compinit`, add guarded Docker completions on `fpath`, e.g. test `-d "$(brew --prefix docker-completion 2>/dev/null)/share/zsh/site-functions"` and `fpath+=` only then. If `brew` or docker-completion absent, append note to `02-DISCUSSION-LOG.md` under `## FIX-09`.
</action>
<acceptance_criteria>
- `grep -n docker ~/dotfiles/.zshrc` shows any Docker `fpath` block at a line number **less than** the first `compinit` line number, OR log documents skip
- OR `02-DISCUSSION-LOG.md` contains `FIX-09` and word `skip` or `absent`
</acceptance_criteria>
</task>

<task id="3" type="execute">
<read_first>
- `~/dotfiles/.zshrc` (Async Load External Tools + Better Terminal Experience sections)
</read_first>
<action>
Move **`eval "$(starship init zsh)"`** to run **after** `eval "$(zoxide init zsh)"` and after `source <(fzf --zsh)` / iterm / bun / cargo as appropriate, satisfying ARCH-06 as the last tool init in that block. Preserve Starship config env var before the eval.
</action>
<acceptance_criteria>
- Line number of `starship init` is greater than line number of `zoxide init` in `.zshrc`
</acceptance_criteria>
</task>

<task id="4" type="execute">
<read_first>
- `~/dotfiles/.zshrc` (tail PATH dedupe section)
</read_first>
<action>
Add at **end** of `.zshrc` (after PERF-05 dedupe block):  
`[[ -f "$HOME/.zshrc.local" ]] && source "$HOME/.zshrc.local"`  
Document in `02-DISCUSSION-LOG.md` that `secrets.sh` remains until user migrates content to `.zshrc.local` (optional follow-up).
</action>
<acceptance_criteria>
- `grep -q 'zshrc.local' ~/dotfiles/.zshrc`
- `zsh -n ~/dotfiles/.zshrc`
</acceptance_criteria>
</task>

</tasks>

<verification>
- `zsh -n ~/dotfiles/.zshrc`
- `grep -n plug ~/dotfiles/.zshrc` — fast-syntax-highlighting last
</verification>

<success_criteria>
- FIX-08, FIX-09, ARCH-06, ARCH-07 addressed or explicitly documented as skipped with reason
</success_criteria>
