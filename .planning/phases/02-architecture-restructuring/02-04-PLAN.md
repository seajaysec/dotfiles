---
phase: 2
plan: "02-04"
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - .zsh.aliases
  - .zsh.functions
  - .planning/phases/02-architecture-restructuring/02-DISCUSSION-LOG.md
autonomous: true
requirements:
  - EFF-01
  - EFF-02
  - EFF-03
---

<objective>
Treat **`~/dotfiles/.zsh.aliases`** and **`~/dotfiles/.zsh.functions`** as first-class scope for **efficiency and maintainability**: inventory hot paths, align high-traffic pipelines with faster patterns where safe (`rg` vs `grep`, avoid parse-time backticks), and document intentional exceptions. Completions efficiency stays coupled to **02-02** (merge + cache path); this plan covers **interactive command** efficiency in the two KEY files you rely on daily.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T1 | Changing alias semantics (BSD vs GNU flags) | high | Prefer `rg` only where flags match; keep `grep` when pipeline requires BSD behavior — log each choice |
| T2 | Breaking `sudo` / clipboard / git aliases | high | After edits: `zsh -n` on both files; spot-check `whence -v` for renamed items |
| T3 | Functions depending on global state (utun8, paths) | medium | Touch only inner command substitutions where isolated |
**Block on:** high.
</threat_model>

<must_haves>
- Written **inventory** in `02-DISCUSSION-LOG.md`: sections `## Aliases inventory` and `## Functions inventory` listing pipeline-heavy entries, `grep` vs `rg` usage counts, and any parse-time `` `...` `` risks (e.g. `audiofix`)
- At least **three** concrete efficiency edits across the two files **or** three documented “no change” decisions with rationale (performance vs portability)
- No regression: `zsh -n ~/dotfiles/.zsh.aliases` and `zsh -n ~/dotfiles/.zsh.functions` (note: functions file may need `zsh -n` after wrapping in empty context — use `zsh -c 'source ...'` if needed)
</must_haves>

<tasks>

<task id="1" type="execute">
<read_first>
- `~/dotfiles/.zsh.aliases` (full file)
- `~/dotfiles/.zsh.functions` (full file)
- `~/dotfiles/.planning/phases/02-architecture-restructuring/02-CONTEXT.md`
</read_first>
<action>
Build the **inventory** in `02-DISCUSSION-LOG.md`: count `grep` / `awk` / `sed` / subshells in aliases; summarize largest functions by line count and external calls (`ping`, `docker`, `ifconfig`). Flag overlap with Phase 4 **FIX-*** items (e.g. `audiofix`, `fff`) — either fix here if trivial or reference “deferred to 04-02” explicitly.
</action>
<acceptance_criteria>
- Discussion log contains both `## Aliases inventory` and `## Functions inventory` headings
- At least one cross-reference to FIX-02, FIX-03, or FIX-04 where applicable
</acceptance_criteria>
</task>

<task id="2" type="execute">
<read_first>
- Inventory section written in task 1
</read_first>
<action>
Apply **efficiency passes** on `.zsh.aliases`: e.g. `lsockT` / `lsockU` use `grep` — evaluate `rg -F` or keep `grep` for `/usr/sbin/lsof` output stability; `myips` already uses `rg`. Prefer changes with **measurable** fewer forks or documented “unchanged for BSD”. Limit scope to **≤15 lines changed** in this task unless a single mechanical replace (e.g. `grep TCP` → `rg -F 'TCP'`) spans more lines — then one atomic commit.
</action>
<acceptance_criteria>
- `git diff --stat .zsh.aliases` shows non-zero change **or** log subsection `## Aliases: no edits` with reason
</acceptance_criteria>
</task>

<task id="3" type="execute">
<read_first>
- `~/dotfiles/.zsh.functions` (sections with tight loops or repeated `ifconfig`/`awk`)
</read_first>
<action>
Apply **targeted** improvements in `.zsh.functions`: e.g. cache repeated `command -v`, replace obvious `grep ... | awk` with single `awk` where behavior identical, add short comments only where non-obvious. **Do not** wholesale restructure the file in this plan — reserve large splits for a later milestone if inventory recommends it; document “Phase 2 scope limit” in log if so.
</action>
<acceptance_criteria>
- `git diff --stat .zsh.functions` shows non-zero change **or** log `## Functions: no edits` with pointer to future phase
</acceptance_criteria>
</task>

</tasks>

<verification>
- `zsh -n ~/dotfiles/.zsh.aliases` (if zsh supports; else `source` in subshell test)
- `zsh -fc 'source ~/dotfiles/.zsh.functions'` smoke (may need minimal env)
</verification>

<success_criteria>
- EFF-01–EFF-03 satisfied; aliases/functions explicitly in Phase 2 scope for efficiency, not only startup
</success_criteria>
