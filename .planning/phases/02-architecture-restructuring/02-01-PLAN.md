---
phase: 2
plan: "02-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .zshenv
  - .zprofile
  - .planning/phases/02-architecture-restructuring/02-DISCUSSION-LOG.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
---

<objective>
Add **tracked** canonical `~/dotfiles/.zshenv` and `~/dotfiles/.zprofile` that match roadmap success criteria, then align the **deployed** `~/.zshenv` and `~/.zprofile` so login shells pick up Homebrew `PATH` before interactive `.zshrc` runs.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T1 | Secrets or machine tokens committed in `.zshenv` | high | Only `typeset -U`, `EDITOR`, `VISUAL`, optional empty-PATH bootstrap — no API keys |
| T2 | `brew shellenv` on Intel vs ARM wrong path | medium | Guard `[[ -x /opt/homebrew/bin/brew ]]` before eval; document Intel path in log if needed |
| T3 | Breaking non-interactive `zsh -c` scripts | medium | Keep `.zshenv` tiny; no `eval` except guarded brew |
**Block on:** high severity.
</threat_model>

<must_haves>
- Repo file `~/dotfiles/.zshenv` exists and contains `typeset -U`, `EDITOR`, and `VISUAL` (≤ 5 non-comment, non-blank lines of configuration)
- Repo file `~/dotfiles/.zprofile` exists and runs guarded Homebrew `brew shellenv` plus a single `typeset -U path` / `path+=` / `export PATH=${(j.:.)path}` (or equivalent) block without duplicating interactive-only tools
- `~/.zshenv` and `~/.zprofile` on disk match the tracked files (or log documents intentional delta with user approval text in discussion log)
</must_haves>

<tasks>

<task id="1" type="execute">
<read_first>
- `~/dotfiles/.planning/phases/02-architecture-restructuring/02-CONTEXT.md`
- `~/dotfiles/.planning/research/ARCHITECTURE.md` (Startup file separation)
- `~/dotfiles/.zshrc` (first 15 lines — Phase 1 PATH bootstrap)
</read_first>
<action>
Create **`~/dotfiles/.zshenv`** in the repo with: (1) short header comment, (2) `export EDITOR` / `export VISUAL` (match current `.zshrc` vim choice unless CONTEXT says otherwise), (3) `typeset -U` for any exported list vars if needed per roadmap, (4) optional **single-line** empty-`PATH` bootstrap **only** if it fits the ≤5-line budget — otherwise leave bootstrap in `.zshrc` for this plan and note deferral in discussion log. No `eval` except explicitly forbidden anyway.
</action>
<acceptance_criteria>
- `test -f ~/dotfiles/.zshenv`
- `grep -q typeset ~/dotfiles/.zshenv && grep -q EDITOR ~/dotfiles/.zshenv && grep -q VISUAL ~/dotfiles/.zshenv`
- `grep -cEv '^#|^$|^[[:space:]]*#' ~/dotfiles/.zshenv` prints a number ≤ 5
</acceptance_criteria>
</task>

<task id="2" type="execute">
<read_first>
- `~/dotfiles/.zshrc` (Path Configuration section ~56–108)
- `~/.zprofile` if it exists on disk (may be outside repo)
</read_first>
<action>
Create **`~/dotfiles/.zprofile`** with: guarded `eval "$(/opt/homebrew/bin/brew shellenv)"` (skip if brew missing), then `typeset -U path` and a **login** `path=(…)` that includes system dirs + Homebrew + user bins you need before `.zshrc` (mirror priorities from current `.zshrc` `path=(` list where appropriate). Export with `export PATH="${(j.:.)path}"` only — never `"${path[*]}"`. Do **not** source interactive-only files here.
</action>
<acceptance_criteria>
- `test -f ~/dotfiles/.zprofile`
- `grep -q brew ~/dotfiles/.zprofile`
- `grep -q 'typeset -U path' ~/dotfiles/.zprofile`
- `grep -F 'path[*]' ~/dotfiles/.zprofile`; expect exit 1
</acceptance_criteria>
</task>

<task id="3" type="execute">
<read_first>
- `~/dotfiles/.zshenv` and `~/dotfiles/.zprofile` as written
</read_first>
<action>
Copy or merge the contents of **`~/dotfiles/.zshenv`** into **`~/.zshenv`** and **`~/dotfiles/.zprofile`** into **`~/.zprofile`** (overwrite or merge conservatively — preserve user-only lines not represented in repo by appending them under a `## Local overrides` comment in the discussion log, not in tracked files). Append a short entry to **`02-DISCUSSION-LOG.md`** describing what was deployed and any machine-specific retention.
</action>
<acceptance_criteria>
- `diff -q ~/dotfiles/.zshenv ~/.zshenv` exits 0 **or** discussion log explains deltas
- Same for `.zprofile` **or** log explains deltas
- `02-DISCUSSION-LOG.md` exists under `.planning/phases/02-architecture-restructuring/`
</acceptance_criteria>
</task>

</tasks>

<verification>
- `zsh -n ~/dotfiles/.zshenv` 2>/dev/null; `zsh -n ~/dotfiles/.zprofile` 2>/dev/null
- `zsh -il -c 'command -v brew'` prints a path when Homebrew present
</verification>

<success_criteria>
- ARCH-01 and ARCH-02 satisfied at file level; ready for `.zshrc` slimming in plan 02-02
</success_criteria>
