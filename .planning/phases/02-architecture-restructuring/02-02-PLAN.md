---
phase: 2
plan: "02-02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - .zshrc
  - completions.zsh
  - .planning/phases/02-architecture-restructuring/02-DISCUSSION-LOG.md
autonomous: true
requirements:
  - ARCH-03
  - ARCH-04
  - ARCH-05
---

<objective>
Merge `completions.zsh` into `~/dotfiles/.zshrc`, eliminate duplicate completion `zstyle` rules, fix **`$ZSH_CACHE_DIR`** usage, ensure **exactly one** `compinit` path, and remove the standalone `completions.zsh` file from the repo per roadmap.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T1 | Duplicate `zstyle` / menu select conflicts | medium | After merge, `grep -c "zstyle ':completion" .zshrc` reviewed; drop duplicates from merged block |
| T2 | `compinit` runs before all `fpath` updates | high | Keep order: all `fpath+=` / plugin sourcing that affects fpath **before** merged completion block |
| T3 | Deleting `completions.zsh` breaks external `source` | medium | Remove `source ~/dotfiles/completions.zsh` from `.zshrc` in same commit as delete |
**Block on:** high.
</threat_model>

<must_haves>
- No `source .*completions.zsh` line remains in `~/dotfiles/.zshrc`
- `completions.zsh` removed from repo **or** replaced with a 3-line stub that only comments “merged into .zshrc” and does nothing (prefer full delete if grep shows no other references)
- Exactly one logical `compinit` invocation for interactive startup (allow `compinit -C` branch only inside same if/fi as today)
- Completion cache path does not reference undefined `$ZSH_CACHE_DIR` — use explicit dir under `$HOME` and `mkdir -p` in task
</must_haves>

<tasks>

<task id="1" type="execute">
<read_first>
- `~/dotfiles/completions.zsh`
- `~/dotfiles/.zshrc` (plugin section through first `zstyle` after compinit)
</read_first>
<action>
Insert the **useful** contents of `completions.zsh` **excluding** its `autoload -U compinit` / `zmodload` lines (`.zshrc` already owns `autoload -Uz compinit`): bring over `fpath+=~/.zfunc`, `_comp_options`, cache `zstyle` lines with **fixed** cache path, and non-duplicate grouping `zstyle`s. Insert this block **immediately before** the existing `autoload -Uz compinit` / daily `compinit` guard. Replace `cache-path $ZSH_CACHE_DIR` with e.g. `$HOME/.cache/zsh` and `mkdir -p` that dir in the same task. Remove any duplicate `zstyle` keys already present later in `.zshrc` (keep the richer `.zshrc` menu/matcher styles where they conflict — delete redundant lines from the merged block).
</action>
<acceptance_criteria>
- `grep -q 'cache-path' ~/dotfiles/.zshrc` and `! grep -q 'ZSH_CACHE_DIR' ~/dotfiles/.zshrc`
- `grep -c '^autoload.*compinit' ~/dotfiles/.zshrc` is 1 (or only inside commented examples — should be 1 live)
</acceptance_criteria>
</task>

<task id="2" type="execute">
<read_first>
- `~/dotfiles/.zshrc`
</read_first>
<action>
Remove the line `source ~/dotfiles/completions.zsh` (or equivalent path). Run `zsh -n ~/dotfiles/.zshrc`. Delete **`~/dotfiles/completions.zsh`**. `grep -r completions.zsh ~/dotfiles` (excluding `.planning` and `.git`) must return no source references — update `README.md` or `install.sh` only if they reference the file.
</action>
<acceptance_criteria>
- `! grep -q 'completions.zsh' ~/dotfiles/.zshrc`
- `test ! -f ~/dotfiles/completions.zsh`
- `zsh -n ~/dotfiles/.zshrc` exits 0
</acceptance_criteria>
</task>

<task id="3" type="execute">
<read_first>
- `~/dotfiles/.planning/codebase/STRUCTURE.md`
</read_first>
<action>
Append **`02-DISCUSSION-LOG.md`** note: merged completions, cache path choice, and confirmation `zsh -i -c exit` still runs (timing optional).
</action>
<acceptance_criteria>
- `grep -qi 'completions' ~/dotfiles/.planning/phases/02-architecture-restructuring/02-DISCUSSION-LOG.md`
</acceptance_criteria>
</task>

</tasks>

<verification>
- `zsh -n ~/dotfiles/.zshrc`
- `test ! -f ~/dotfiles/completions.zsh`
</verification>

<success_criteria>
- ARCH-04, ARCH-05 met; `.zshrc` is the single source for completion init
</success_criteria>
