---
phase: 1
plan: "01-03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - .zshrc
  - .planning/phases/01-critical-startup-fixes/01-DISCUSSION-LOG.md
autonomous: true
requirements:
  - PERF-06
  - PERF-07
  - PERF-05
  - PERF-01
---

<objective>
Remove the MANPATH subshell pipeline from `.zshrc`, consolidate stale `.zcompdump` files, and prove PERF-01 (startup < 200ms), PERF-05 (no duplicate PATH segments), and roadmap hygiene criteria.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T1 | `man` breaks after MANPATH removal | low | Run `man man` once; if failure, restore MANPATH to static safe default documented in log |
| T2 | `rm` wrong zcompdump | medium | Only delete files matching `~/.zcompdump*` that are not the active dump **after** identifying active name from `compinit` call in `.zshrc` (default `~/.zcompdump`) |
**Block on:** high — none expected; document if `man` regresses.
</threat_model>

<must_haves>
- No lines between a line containing `MANPATH=$(manpath` and the next `export MANPATH` in `.zshrc` (entire block removed per CONTEXT D-04)
- `echo "$PATH" | tr ':' '\n' | sort | uniq -d` produces empty output from interactive zsh
- Median of three `time zsh -i -c exit` real samples is under 0.200 seconds (200ms)
</must_haves>

<tasks>

<task id="1" type="execute">
<read_first>
- `~/dotfiles/.zshrc` (lines 87–94 region)
- `~/dotfiles/.planning/phases/01-critical-startup-fixes/01-CONTEXT.md` (D-04)
</read_first>
<action>
Delete the entire MANPATH block starting at the line `MANPATH=$(manpath` through `export MANPATH` inclusive (currently approximately lines 90–93). Do not add a replacement unless `man man` fails in verification task.
</action>
<acceptance_criteria>
- `grep -n 'MANPATH=\$(manpath' ~/dotfiles/.zshrc` exits 1 (no match)
- `grep -n "export MANPATH" ~/dotfiles/.zshrc` exits 1 (no match)
</acceptance_criteria>
</task>

<task id="2" type="execute">
<read_first>
- `~/dotfiles/.zshrc` (compinit / zcompdump section ~147–153)
</read_first>
<action>
List all files matching `$HOME/.zcompdump*` via `ls -1 ~/.zcompdump* 2>/dev/null`. Keep exactly one primary dump file `~/.zcompdump` (the name used in `.zshrc` `stat` check). Delete extra numbered or dated variants (e.g. `.zcompdump-hostname-5.9`) only after confirming they are not the only dump. If only one file exists, no deletions — append note to `01-DISCUSSION-LOG.md`: `## zcompdump: single file already`.
</action>
<acceptance_criteria>
- `ls ~/.zcompdump* 2>/dev/null | wc -l` prints `1` **or** discussion log contains `## zcompdump: single file already`
- `01-DISCUSSION-LOG.md` contains a line with substring `zcompdump` added by this task
</acceptance_criteria>
</task>

<task id="3" type="execute">
<read_first>
- `~/dotfiles/.planning/ROADMAP.md` (Phase 1 success criteria 3–4)
</read_first>
<action>
Run interactive checks from a login-equivalent shell: `zsh -i -c 'echo "$PATH" | tr ":" "\n" | sort | uniq -d | wc -l'` must print `0`. Run `for i in 1 2 3; do time zsh -i -c exit; done 2>&1` and compute median real time; append section `## Startup benchmark (plan 01-03 final)` to `01-DISCUSSION-LOG.md` with the three reals and the word `median=` followed by the median value. If median ≥ 0.200, do not fake — document gap and list remaining hotspots from `zsh -xv` sample (optional) for Phase 2.
</action>
<acceptance_criteria>
- `zsh -i -c 'echo "$PATH" | tr ":" "\n" | sort | uniq -d | wc -l'` outputs exactly `0`
- `01-DISCUSSION-LOG.md` contains literal substring `## Startup benchmark (plan 01-03 final)`
- Same section contains literal substring `median=`
</acceptance_criteria>
</task>

</tasks>

<verification>
- `man man | head -1` returns non-empty first line (manual sanity) **or** log documents acceptable failure
- Roadmap success criteria 4–5 checked via commands in task 3
</verification>

<success_criteria>
- PERF-06: no MANPATH subshell at source time
- PERF-07: at most one `.zcompdump` artifact policy satisfied or documented
- PERF-05: duplicate PATH segments eliminated (`uniq -d` empty)
- PERF-01: median `time zsh -i -c exit` real < 0.200s **or** explicit documented gap for follow-up
</success_criteria>
