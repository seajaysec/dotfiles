---
phase: 1
plan: "01-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .zshrc
  - .planning/phases/01-critical-startup-fixes/01-DISCUSSION-LOG.md
autonomous: true
requirements:
  - PERF-02
  - PERF-01
---

<objective>
Remove any Cursor agent `eval "$(… shell-integration …)"` from zsh startup files that are actually loaded, and record an interactive startup timing baseline after that change.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T1 | Wrong file edited (deployed shell not dotfiles) | medium | List `~/.zshrc` and confirm it resolves to `~/dotfiles/.zshrc` or copy changes to the loaded file |
| T2 | Accidental removal of unrelated eval | low | Only delete lines matching `agent shell-integration` or documented Cursor agent pattern |
**Block on:** high severity — none for this plan.
</threat_model>

<must_haves>
- No `agent shell-integration` string in `~/dotfiles/.zshrc` or `~/.zshrc`
- One recorded `time zsh -i -c exit` real elapsed time (seconds) in execution notes
</must_haves>

<tasks>

<task id="1" type="execute">
<read_first>
- `~/dotfiles/.zshrc`
- `~/dotfiles/.planning/phases/01-critical-startup-fixes/01-CONTEXT.md`
</read_first>
<action>
Run `grep -nEi 'agent shell-integration|cursor.*agent' ~/dotfiles/.zshrc ~/.zshrc 2>/dev/null` and capture output. If any match exists in the file that zsh loads for interactive sessions (prefer `~/dotfiles/.zshrc` when `~/.zshrc` is a symlink to it), remove only the matching `eval "$(…)"` block (including continuation lines inside the quoted command substitution if present). Do not remove `eval "$(starship init zsh)"`, `eval "$(zoxide init zsh)"`, or pyenv eval. If there are zero matches in both paths, make no edit and state "PERF-02 already satisfied" in the commit body.
</action>
<acceptance_criteria>
- Shell command `grep -Ei 'agent shell-integration' ~/dotfiles/.zshrc` returns exit code 1
- If `test -f ~/.zshrc`, then `grep -Ei 'agent shell-integration' ~/.zshrc` returns exit code 1
- Git commit message contains either `PERF-02` or the phrase `already satisfied`
</acceptance_criteria>
</task>

<task id="2" type="execute">
<read_first>
- `~/dotfiles/.planning/ROADMAP.md` (Phase 1 success criterion 1)
</read_first>
<action>
From the repo root, run exactly: `time zsh -i -c exit` three times in a row. Append a new section `## Startup benchmark (plan 01-01)` to `~/dotfiles/.planning/phases/01-critical-startup-fixes/01-DISCUSSION-LOG.md` (create if missing) listing the three real times in milliseconds or seconds as printed by `time`. Do not claim PERF-01 pass/fail here unless all three are under 0.2s real — just record numbers.
</action>
<acceptance_criteria>
- `01-DISCUSSION-LOG.md` contains the literal heading `## Startup benchmark (plan 01-01)`
- Same file contains three numeric timings on separate lines after that heading
- File path is exactly `.planning/phases/01-critical-startup-fixes/01-DISCUSSION-LOG.md` under repo root
</acceptance_criteria>
</task>

</tasks>

<verification>
- `grep -Ei 'agent shell-integration' ~/dotfiles/.zshrc`; expect exit 1
- `tail -n 30 ~/dotfiles/.planning/phases/01-critical-startup-fixes/01-DISCUSSION-LOG.md` shows benchmark section
</verification>

<success_criteria>
- PERF-02 satisfied: no Cursor agent shell-integration eval in tracked or deployed zshrc used for verification
- PERF-01 partially instrumented: baseline timings recorded for comparison after plans 01-02 and 01-03
</success_criteria>
