---
phase: 1
plan: "01-02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .zshrc
  - .planning/phases/01-critical-startup-fixes/01-DISCUSSION-LOG.md
autonomous: false
requirements:
  - PERF-03
  - PERF-04
  - PERF-05
  - PERF-01
---

<objective>
Stop double-sourcing `cargo` and `bun` from `~/.zshenv` while keeping a single interactive load in `.zshrc`, and reduce redundant `export PATH=` churn in `~/dotfiles/.zshrc` without dropping tool locations required by CONTEXT.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T1 | `cargo` or `bun` missing from PATH in new shells | high | After edits, `command -v cargo` and `command -v bun` must succeed from `zsh -i -c 'command -v cargo; command -v bun'` |
| T2 | Editing wrong zshenv | medium | Only edit `$HOME/.zshenv`; if file absent, create minimal file without cargo/bun lines (or skip with documented reason) |
**Block on:** high — broken `command -v cargo` or `command -v bun` after change.
</threat_model>

<must_haves>
- `~/.zshenv` either absent or contains no `source`/`eval` line for `~/.cargo/env` and none for `~/.bun/_bun`
- `~/dotfiles/.zshrc` still contains both: a line sourcing `$HOME/.cargo/env` and a line sourcing `$HOME/.bun/_bun` (or equivalent `[ -f … ] && source …` guards)
- Fewer `export PATH=` lines than current baseline: record `baseline_export_path_count=$(grep -c '^export PATH=' ~/dotfiles/.zshrc)` in `01-DISCUSSION-LOG.md` before edits; after edits the same count must be **strictly lower** than baseline, and **at most 3**, by merging redundant `export PATH="${path[*]}"` passes without breaking pyenv shims order
</must_haves>

<tasks>

<task id="1" type="execute">
<read_first>
- `~/dotfiles/.zshrc` (PATH and bun/cargo sections)
- `~/dotfiles/.planning/phases/01-critical-startup-fixes/01-CONTEXT.md` (D-02, D-03)
</read_first>
<action>
If `$HOME/.zshenv` exists, open it and delete any lines that source `~/.cargo/env` or `$HOME/.cargo/env`, and any lines that source `~/.bun/_bun` or `$HOME/.bun/_bun` or run bun’s install env hook. If `~/.zshenv` does not exist, skip deletion and add a one-line comment to `01-DISCUSSION-LOG.md`: `## zshenv: absent — PERF-03/04 N/A for cargo/bun dedupe from zshenv`. Preserve all other `~/.zshenv` content unrelated to cargo/bun.
</action>
<acceptance_criteria>
- Shell: `test ! -f ~/.zshenv && echo absent` OR `test -f ~/.zshenv && ! grep -qF '.cargo/env' ~/.zshenv && ! grep -qF '.bun/_bun' ~/.zshenv`
- `01-DISCUSSION-LOG.md` contains either edited zshenv note or the exact substring `PERF-03/04 N/A`
</acceptance_criteria>
</task>

<task id="2" type="execute">
<read_first>
- `~/dotfiles/.zshrc` (full PATH section lines 52–113)
</read_first>
<action>
Append to `01-DISCUSSION-LOG.md` a line `baseline_export_path_count=N` where N is `grep -c '^export PATH=' ~/dotfiles/.zshrc` before changes. Refactor PATH updates: keep the pyenv-required line `export PATH="$PYENV_ROOT/shims:$PYENV_ROOT/bin:$PATH"` intact. Merge redundant `export PATH="${path[*]}"` so that after edits `grep -c '^export PATH=' ~/dotfiles/.zshrc` is **≤ 3** and **< N**. Prefer one final `export PATH="${path[*]}"` after all `path+=` blocks (Go, Bun). Optionally fold line 15 `export PATH="/opt/homebrew/bin:$PATH"` into the `path=(` array instead of a separate export if it reduces count without losing `/opt/homebrew/bin` on PATH.
</action>
<acceptance_criteria>
- `grep -c '^export PATH=' ~/dotfiles/.zshrc` prints a number ≤ 3 and strictly less than the logged `baseline_export_path_count`
- `grep -qF '.cargo/env' ~/dotfiles/.zshrc` exits 0
- `grep -qF '.bun/_bun' ~/dotfiles/.zshrc` OR `grep -qF '"$HOME/.bun/_bun"' ~/dotfiles/.zshrc` exits 0
</acceptance_criteria>
</task>

<task id="3" type="execute">
<read_first>
- `~/dotfiles/.zshrc`
</read_first>
<action>
Run `zsh -i -c 'command -v cargo >/dev/null && command -v bun >/dev/null && echo OK'`. Expected stdout is exactly `OK` followed by newline. If it fails, revert PATH refactor until OK.
</action>
<acceptance_criteria>
- Previous command stdout is exactly `OK`
- `time zsh -i -c exit` real time is strictly less than before plan 01-02’s worst of three timings recorded in `01-DISCUSSION-LOG.md` **or** document in log why (e.g. network) with median comparison
</acceptance_criteria>
</task>

</tasks>

<verification>
- `zsh -i -c 'command -v cargo; command -v bun'` prints two absolute paths
- `grep -c '^export PATH=' ~/dotfiles/.zshrc` is ≤ 3
</verification>

<success_criteria>
- PERF-03 and PERF-04: no double-load from zshenv; interactive still loads once each from `.zshrc`
- PERF-05: reduced `export PATH=` repetition per acceptance counts
- PERF-01: trend toward &lt; 200ms (final gate in plan 01-03)
</success_criteria>
