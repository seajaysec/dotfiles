---
phase: 8
plan: "08-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/EXTERNAL-PATTERNS.md
  - .planning/ROADMAP.md
  - .planning/phases/08-external-patterns-research/08-DISCUSSION-LOG.md
autonomous: true
requirements:
  - EXT-01
  - EXT-02
  - EXT-03
---

<objective>
Satisfy **EXT-01**–**EXT-03**: survey public dotfiles and articles from users with similar stacks; fill **`EXTERNAL-PATTERNS.md`** with an adopt / reject / defer table; merge **≤2** clearly safe improvements into the repo **or** add explicit ROADMAP / backlog bullets for larger items.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T1 | Copying incompatible licensing or secrets | high | Only patterns; no pasted tokens; cite sources |
| T2 | Breaking portability by adopting Linux-only snippets | medium | macOS-only gate in each adopt row |
**Block on:** high.
</threat_model>

<must_haves>
- ≥5 external references summarized (URLs) in discussion log or foot of `EXTERNAL-PATTERNS.md`
- Adopt / reject / defer table non-empty in `EXTERNAL-PATTERNS.md`
- EXT-03: two items applied in-repo **or** two backlog/roadmap entries with short rationale
</must_haves>

<tasks>

<task id="1" type="execute">
<read_first>
- `.planning/ROADMAP.md` Phase 8 section
- `~/dotfiles/.zshrc` (first 80 lines) — current stack fingerprint
</read_first>
<action>
Web/repo survey: collect ≥5 links (GitHub dotfiles, “macOS zsh” posts, Starship/Zap examples). Append **Reference list** to `EXTERNAL-PATTERNS.md` or `08-DISCUSSION-LOG.md`.
</action>
<acceptance_criteria>
- Reference list has ≥5 entries with one-line stack overlap each
</acceptance_criteria>
</task>

<task id="2" type="execute">
<read_first>
- Reference list from task 1
</read_first>
<action>
Fill **Adopt / Reject / Defer** tables in `EXTERNAL-PATTERNS.md` with concrete patterns (e.g. compinit flags, `zsh-defer`, brew bundle, symlink layouts). Mark each row with effort (S/M/L).
</action>
<acceptance_criteria>
- **Adopt** has ≥1 row; **Reject** and **Defer** each have ≥1 row or a single `*(none — rationale in log)*` row
</acceptance_criteria>
</task>

<task id="3" type="execute">
<read_first>
- Adopt table
</read_first>
<action>
Implement **≤2** small safe adopts (config-only, no new secrets) **or** add ROADMAP overview bullets / `.planning` backlog notes for deferred work. Commit with message scoped to Phase 8.
</action>
<acceptance_criteria>
- EXT-03 satisfied per `REQUIREMENTS.md`
</acceptance_criteria>
</task>

</tasks>

<verification>
- `EXTERNAL-PATTERNS.md` renders as intended markdown
- `git status` clean after commit(s)
</verification>
